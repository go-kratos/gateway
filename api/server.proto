syntax = "proto3";

package kratos.gateway;

import "go-kratos/gateway/api/rule.proto";

option go_package = "github.com/go-kratos/gateway/api";

// 网关server配置
message Server {
    // gateway server本身的静态配置
    string address = 1;

    // 可动态替换的service rule配置
    Rule rule = 2;
}