syntax = "proto3";

package gateway.middleware.dyeing.v1;

option go_package = "github.com/go-kratos/gateway/api/gateway/middleware/dyeing/v1";

// Dyeing middleware config.
message Dyeing {
  string header = 1;
  string label = 2;
}
